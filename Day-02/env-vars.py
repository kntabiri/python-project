# how to create an environment variable for sensitive information

import os
print (os.getenv("password"))
print (os.getenv("apitoken"))
print (os.getenv("username"))

# execute python3 env.vars.py in the terminal
